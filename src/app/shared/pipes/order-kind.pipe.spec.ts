import { OrderKindPipe } from './order-kind.pipe';

describe('OrderKindPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderKindPipe();
    expect(pipe).toBeTruthy();
  });
});
